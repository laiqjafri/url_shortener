<div class="row-fluid">
  <div class="col-md-3"></div>
  <div class="col-md-6 well">
    <%= form_for @url, :url => urls_path, :html => {:class => 'form-inline'} do |form| %>
      <%= render :partial => 'fields', :locals => {:form => form} %>
    <% end %>
    <%= render :partial => 'new_url' %>
  </div>
  <div class="col-md-3"></div>
</div>

<div class="row-fluid">
  <%= render :partial => 'urls' %>
</div>
